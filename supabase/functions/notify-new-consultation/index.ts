import { serve } from "https://deno.land/std@0.224.0/http/server.ts";
serve(async (req)=>{const body=await req.json(); const record=body.record||body; const key=Deno.env.get("RESEND_API_KEY"); const to=Deno.env.get("NOTIFY_EMAIL"); if(!key||!to)return new Response("missing secrets",{status:500}); const html=`<h2>새 Re2O 상담이 등록되었습니다.</h2><p><b>상담번호:</b> ${record.public_code}</p><p><b>별명:</b> ${record.nickname}</p><p><b>유입:</b> ${record.source}${record.ai_tool?` / ${record.ai_tool}`:""}</p><p><b>검색 주제:</b> ${record.search_topic}</p><p><b>질문:</b> ${record.question}</p><p><a href="https://re2omyd.com/admin.html">관리자 페이지에서 답변하기</a></p>`; const r=await fetch("https://api.resend.com/emails",{method:"POST",headers:{Authorization:`Bearer ${key}`,"Content-Type":"application/json"},body:JSON.stringify({from:Deno.env.get("FROM_EMAIL")||"MyD Consultation <onboarding@resend.dev>",to:[to],subject:`[마이디피부과] 새 Re2O 상담 ${record.public_code}`,html})}); return new Response(await r.text(),{status:r.status});});
